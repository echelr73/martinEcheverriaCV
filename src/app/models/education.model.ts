export class Education {
    degree: string;
    institution: string;
    period: string;
    description: string;
  
    constructor(degree: string, institution: string, period: string, description: string) {
      this.degree = degree;
      this.institution = institution;
      this.period = period;
      this.description = description;
    }
  }
  